/**
 * Mesmas variáveis definidas em `app/globals.css` (`--chart-1` … `--chart-5`).
 * Use nos gráficos (Recharts etc.) sem repetir strings na página.
 */
export const chartPalette = [
  "var(--chart-1)",
  "var(--chart-2)",
  "var(--chart-3)",
  "var(--chart-4)",
  "var(--chart-5)",
] as const
